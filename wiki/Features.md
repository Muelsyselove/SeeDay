# 功能介绍

SeeDay 提供了一套完整的个人时间追踪和可视化解决方案，以下是核心功能的详细介绍。

## 实时活动追踪

### 功能描述

SeeDay 通过安装在设备上的轻量级 Agent 程序，实时监测并记录用户当前正在使用的应用程序。

### 核心能力

- **前台应用检测**：自动识别当前处于前台的应用程序
- **应用名称映射**：将应用路径映射为人类可读的应用名称
- **实时更新**：Agent 定期向服务器报告当前活动状态
- **多设备支持**：支持同时追踪多个设备的活动数据

### Agent 平台支持

| 平台 | 状态 | 技术栈 |
|------|------|--------|
| Windows | ✅ 已支持 | Python + Win32 API + pystray |
| macOS | 🔨 开发中 | Python + AppleScript + pystray |
| iOS | 🔨 开发中 | 计划中 |
| Android | 🔨 开发中 | 计划中 |

### Windows Agent 功能

- 系统托盘常驻运行
- AFK（离开键盘）检测
- 看视频/听歌时自动豁免追踪
- 音乐播放器检测（Spotify、QQ音乐、网易云等）
- 三级隐私系统支持

## 可视化仪表盘

### 功能描述

精美的 Web 界面，实时展示各设备的活动状态和使用统计。

### 界面元素

- **当前状态展示**：显示所有设备的实时活动状态
- **活动时间线**：按时间顺序展示每日活动记录
- **应用使用统计**：Top 应用使用时长图表
- **设备卡片**：每个设备一张卡片，显示在线状态和活动信息

### 主题系统

SeeDay 支持多种主题风格，可自由切换：

| 主题 | 风格 | 说明 |
|------|------|------|
| 默认主题 | 现代简洁 | 干净的现代设计风格 |
| 水墨主题 | 中国传统 | 水墨国风，宣纸质感，朱砂红点缀 |
| 更多主题 | 持续添加 | 社区贡献主题 |

主题切换方式：
- 在仪表盘设置中切换
- 使用快捷键快速切换

### 响应式设计

- 桌面端：完整的双栏/多栏布局
- 平板端：自适应单栏布局
- 手机端：优化的移动端体验

## AI 每日总结

### 功能描述

每晚自动汇总当日活动数据，调用 AI API 生成简洁的中文活动总结。

### 工作流程

1. 每晚 21:00 定时触发
2. 汇总当日所有活动数据
3. 调用 AI API 生成 50-80 字中文总结
4. 存储到数据库并在仪表盘展示

### 支持的 AI 提供商

| 提供商 | API 端点 | 推荐模型 |
|--------|---------|---------|
| OpenAI | `https://api.openai.com/v1/chat/completions` | gpt-4o-mini |
| Claude | 代理地址 | claude-3-haiku-20240307 |
| DeepSeek | `https://api.deepseek.com/chat/completions` | deepseek-chat |
| 本地 Ollama | `http://localhost:11434/v1/chat/completions` | qwen2.5:7b |

### 配置方式

在 `.env` 文件中设置：

```
AI_API_URL=https://api.openai.com/v1/chat/completions
AI_API_KEY=sk-...
AI_MODEL=gpt-4o-mini
```

> 不配置 AI 时，AI 总结功能静默跳过，不影响其他功能。

## 隐私保护

### 三级隐私系统

| 级别 | 说明 |
|------|------|
| SHOW | 完全公开，显示应用名称和活动描述 |
| BROWSER | 仅显示浏览器活动，隐藏具体页面标题 |
| HIDE | 完全隐藏，不显示任何活动信息 |

### 隐私特性

- 数据完全本地存储（SQLite 数据库）
- 灵活的隐私设置，可按应用设置隐私级别
- 敏感窗口标题自动过滤
- 不上传任何个人数据到云端
- 敏感词过滤系统（NSFW 内容过滤）

## 多设备支持

### 功能描述

支持同时管理多个设备的活动数据，在统一的仪表盘中查看所有设备状态。

### 设备管理

- 每个设备独立的 Token 认证
- 设备名称自定义
- 设备类型标识（Windows/macOS/其他）
- 在线/离线状态实时显示

### 离线处理

- 设备离线时显示友好的离线状态
- 自动检测长时间离线
- 夜间模式自动切换（设备全离线时）

## 数据统计

### 活动聚合

- 相同应用的连续活动自动合并
- 按应用分组的时间线展示
- "此刻"实时汇总置顶显示

### 数据清理

- 自动清理过期数据
- AI 总结 7 天自动清理
- 可配置的保留策略
